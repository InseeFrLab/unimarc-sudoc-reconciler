# Port d'écoute du serveur (par défaut 3000)
PORT=3000

# Mettre à "production" pour servir le front compilé depuis dist/
# au lieu de monter Vite en middleware. `npm run start` le fait déjà.
# NODE_ENV=production

# Le LLM des suggestions RAMEAU (endpoint, clé d'API, modèle) se configure
# UNIQUEMENT depuis l'interface, panneau « Configuration LLM » du tableau de
# bord. Aucune variable d'environnement n'est lue pour cela.
