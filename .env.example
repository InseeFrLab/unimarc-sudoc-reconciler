# Port d'écoute du serveur (par défaut 3000)
PORT=3000

# --- LLM (facultatif : suggestions de vedettes RAMEAU) ---
# L'endpoint et la clé se saisissent aussi depuis l'interface.
# Valeurs par défaut pointant vers le LLM du SSP Cloud :
# LLM_ENDPOINT=https://llm.lab.sspcloud.fr/api/chat/completions
